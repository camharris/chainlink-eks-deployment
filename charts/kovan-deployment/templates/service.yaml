apiVersion: v1
kind: Service
metadata:
  name: {{ include "kovan-deployment.fullname" . }}
  labels:
    {{- include "kovan-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kovan-deployment.selectorLabels" . | nindent 4 }}
