apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kovan-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "kovan-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kovan-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
